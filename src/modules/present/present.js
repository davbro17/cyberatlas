//import Worker from "worker-loader!./present.worker.js"

export default {
    //worker: new Worker(),
    prompts: [
        "create a slide presentation",
        "present slides"
    ],
    parallel: false
}